# Gonio GPU-optimized Python helpers

`gonio_export.py` adds NumPy/caching/EXR support to the stable workflow;
`run_gonio_batch.py` performs angle sweeps and `profile_gonio_timing.py`
prints stage timings.
