# Gonio stable Python helpers

`gonio_export.py` runs a measurement and writes the stable BMP/`.dat` bundle;
`run_gonio_batch.py` scans multiple incident angles. Both can also be imported
from notebooks after adding `src` to `PYTHONPATH`.
